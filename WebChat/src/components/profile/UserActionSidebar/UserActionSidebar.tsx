import { Button } from "../../UI";

export function UserActionSidebar() {
    return (
        <>
        <Button onClick={()=>{}}>No sound</Button>
        <Button onClick={()=>{}}>Send contact</Button>
        <Button color="destructive" onClick={()=>{}}>Delete chat</Button>
        </>
    )
}